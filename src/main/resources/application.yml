# application-dev.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/javue?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "0000"
  jpa:
    show-sql: true   # ???? SQL ????
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true   # SQL ???/?????? ???
        highlight_sql: true  # (Hibernate 6+) ANSI ?? ??
